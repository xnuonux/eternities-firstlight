# Interchangeable prototype views

Base: `117ec56c9cd8b24b1f81f43e38422d49c3db6880`, open PR #6. Branch: `gameplay/interchangeable-views`, using the existing isolated primary worktree. Dom approved the visual direction ("looks great, honestly, continue development") and explicitly wants both the 2.5D diorama and third-person styles to remain interchangeable. This is visual direction approval, not a completed fresh/returning gameplay report.

The next bounded improvement makes that choice work comfortably throughout the existing outing. The four modes remain: third person (internal `adventure`), diorama (`follow`), tactical and wide. V switches between third person and the last selected diorama mode. R resets the current mode. Each mode remembers its own orbit/elevation/zoom; overhead zoom is relative to the authored scene scale so entering a house does not reuse a valley-sized framing. The visible controls explain the two styles and the FOV control identifies itself as third-person-only.

Ownership: `core.js` validates optional version-1 view preferences; `app.js` captures/restores framing, controls and transitions; `rpg-ui.js` names modes and routes reset input. World schema/key9, adventure6 and starter1 stay unchanged. Older saves get empty view profiles and default to the existing camera choice. Invalid optional profiles fall back without damaging gameplay/creative state. No quest, reward, combat, motion-shake, renderer, realm or economy changes.

Acceptance: actual UI switching preserves view framing, selection/combat/quest state, scene-relative zoom and reload preferences; V/R do not leak through menus or text fields; third-person FOV remains independent of diorama zoom. Existing perspective picking and overhead controls keep their browser checks. Tests cover real callers, not just preference helpers. Regenerate identical HTML, record a short actual comparison, and return a stacked PR with exact commits and fresh verification. The previously queued PR6 workflow remains a separate pending external result; do not duplicate or weaken its tests.
