# Outing readability: bounded refinement

2026-09-14, authorized autonomous window ending07:00 Central. Branch `gameplay/outing-readability`, base PR #12 at447f49b2d06962c23f992befe043d679711892c9. The prior milestone implements optional classes; rendered/source inspection now finds three small discovery/presentation gaps: Oren's service view does not link to the new choice, the roster/equipment heading does not show chosen identity, and the active survey HUD repeats objective counts in adjacent lines.

Add a natural Oren→Class path link, display the saved class in equipment and roster, and show each survey count once alongside useful navigation text. The chosen-path page should offer the existing legitimate practice-route action and field guide. Reuse `PursuitUI.routeButton('practice')`; it walks to the sign/target and still requires E to enter, with no teleport or combat/reward mutation. No new schema, skill, equipment, class rule, camera framing, campaign gate or payout is introduced.

Extend the existing class browser journey with visible Oren discovery, choice preview safety, path navigation, truthful inactive-character badges and nonduplicated survey HUD. Source checks and the relevant class/roster/pursuit browser gates precede a separate stacked review PR and fresh remote-clone verification. Record rendered evidence and a separate actual RTX browser-frame measurement on the final build. Human enjoyment/pacing remains pending. Do not start further features after11:15UTC; finish delivery by12:00UTC.

Integration review found a related import boundary gap while preparing the playable handoff: a delayed character file could open its preview after switching lives, and an earlier slow file could supersede a newer selection. Reproduce through the actual file picker with synthetic delayed reads, then bind completion to the originating simulation, active character, roster revision and latest file request. Refusal must leave both saved worlds untouched. Keep source-byte checks, Web Locks and explicit import confirmation intact; no new save schema is needed.

The final local-play handoff needs a direct Windows launcher for the already tested HTTP origin. Add a small portable Python server and Windows entry point, preserving port 8780. Serve only cached, identical generated game bytes; use loopback exclusively. Reuse only an exact existing build and refuse a conflicting listener without killing processes or silently changing ports. Verify wrong-build, missing-build, routing, redirect and exclusive-binding behavior, then exercise the actual Windows entry point. This is local delivery tooling, with no save read/write or new gameplay feature.
