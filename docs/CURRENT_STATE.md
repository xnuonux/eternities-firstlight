# Current checkpoint: chosen Hunter and Magician paths

Updated 2026-09-14 during Dom's authorized autonomous window ending 07:00 Central. Branch `gameplay/chosen-class-experiment`, stacked against PR #11 (`gameplay/character-roster`) at **93cf94ec2327d700007534e458293aff408acf07**. Main remains unmerged. PR #11 has an exact-head fresh remote-clone verification; its roster remains the foundation of this change. PR #9's Draft D archive is direction, not the gameplay tree.

Start with [the bounded class contract](development/CHOSEN_CLASS_TASK_2026-09-14.md), [results](development/CHOSEN_CLASS_RESULTS_2026-09-14.md), [NEXT_TASK](NEXT_TASK.md), [PLAYTEST_NOTES](PLAYTEST_NOTES.md), and [DECISIONS](DECISIONS.md). The implementation PR records its exact pushed head and final fresh-clone receipt. Older camera, equipment, roster and campaign checkpoints remain in dated records and Git history.

**Play locally:** serve this checkout with `python -m http.server 8780 --bind 127.0.0.1` and open `http://127.0.0.1:8780/`. Keep the same origin/profile for existing saves; use an isolated profile for synthetic evidence. HTTP is the verified character-library origin. `OPEN_REALM_WINDOWS.cmd` retains the separate offline-file launch path.

1. At Oren's workshop, collect the initial expedition kit. Open **More → Class path**, or **Character → Path**. Compare Hunter and Magician, then explicitly preview and confirm a once-per-character choice. You may continue entirely unassigned. Both paths retain blade, bow, companion, profession and soul options.
2. **Hunter / Quarry mark / X:** 15 stamina, 8-second cooldown, visible target within 11 paces. Mark lasts 8 seconds. The next confirmed player weapon impact against that encounter adds 50% of the impact, rounded, capped at 24 extra. A missed/blocked arrow, another target, companion or soul strike does not spend it.
3. **Magician / Arcane flare / X:** 25 stamina, 10-second cooldown, visible target within 8 paces. An instant focused spell deals `round(8 + current attack × 0.75)` through the normal damage rules. Violet cast/impact geometry and confirmed hit feedback show the result. It is a spell, not a travelling arrow.
4. Try the riverbank practice bundle, then the existing repeat materials survey. Tab/click select; 1 autoattacks while you control movement; 2–6 and Space retain their meanings. X adds a technique without replacing a previous control. The timed archery-medal court excludes these class techniques.
5. **More → Characters** still holds three independent complete local lives. Creation starts fresh, imports add a slot after preview, switching commits the outgoing checkpoint first, and exact-name deletion affects only an inactive slot. One character's class never assigns the others.

**Equipment and cameras remain:** the real field guide pins one project; deliberately accepted surveys pay 3 ore, 4 sunmarks, 2 fibre and 0 XP; finite fittings preserve weapon identity and sockets. Oren's original once-only quest stays separate. V swaps third person/diorama; R resets current framing. Settings keeps FOV and reduced motion; each character retains its camera preferences.

**Migration:** adventure schema **6 → 7** introduces required nested `classPath` v1 `{version:1,choice:null|hunter|magician,readyAt:number}`. Existing worlds migrate to **unassigned**, preserving stored XP and all other canonical fields. Cooldown persists on the character's elapsed clock; temporary marks clear at scene/run/reload. World/key9, character envelope1, starter1, pursuit1, arsenal1 and cameraViews1 remain unchanged. The current save destination receives schema7 on saving; older schema6 builds refuse newer worlds instead of silently stripping their class. Managed roster saves keep the previous legacy key's bytes. No save keys are deleted and personal saves were not inspected.

The prototype offers one technique per chosen class, not a skill tree or class-specific equipment catalogue. Numerical balance, class names and permanent-choice UX await Dom's playtest. No rare-pet allocation, offline-loss rules, paid power, multiplayer or construction-scale decision is resolved here. No merge, tag, Unreal rewrite or public deployment.
