# Current checkpoint: separate character lives

Updated 2026-09-14. Branch: `gameplay/character-roster`, stacked against PR #10 (`gameplay/upgrade-worth-hunting`) at `af332a2dcc1affa35aaf0efd7cdcdd5086fc8054`. Dom authorized autonomous development until 07:00 America/Chicago on 2026-09-14. This bounded continuation follows Draft D's independent-character milestone. Main remains unmerged.

Start with [the character contract](development/CHARACTER_ROSTER_TASK_2026-09-14.md), [results](development/CHARACTER_ROSTER_RESULTS_2026-09-14.md), [NEXT_TASK](NEXT_TASK.md), [PLAYTEST_NOTES](PLAYTEST_NOTES.md), and [DECISIONS](DECISIONS.md). The gameplay PR and final verification receipt identify the exact pushed head. Earlier equipment, camera, starter, and import checkpoints remain in dated records and Git history. PR #9's archive at `4f225b2ddce3a1fedafb3b97c92d6e58d8f7e952` is design input, not a replacement source tree.

**Play locally:** from this checkout run `python -m http.server 8780 --bind 127.0.0.1`, then open `http://127.0.0.1:8780/`. Keep the same browser profile and origin for existing play. Use an isolated profile for synthetic fixtures. `OPEN_REALM_WINDOWS.cmd` still opens the offline HTML; native HTTP-origin persistence is the tested character-library path.

1. Open **More → Characters**, or the Characters tab in the character workspace. Your existing world is the first character. Creating another character is an explicit action; merely opening the page does not migrate the save.
2. Name a new character and choose a palette. It starts with a fresh valley, no inherited equipment or campaign progress. Up to three characters each own their complete local world: gear, quest/project claims, XP, soul history, companion, home, crops, notebook, music, schedules, and both camera preferences.
3. **Play this character** saves the outgoing safe outdoor checkpoint and selection together before restoring the incoming world. Attacks, walking, building, open dialogs, and music stop. Sound must be enabled again. Inactive worlds do not advance while another character is played.
4. **Import character** previews a valid ordinary world JSON, then adds a new slot after confirmation. **Export** writes a compatible ordinary world JSON for the selected character. Existing characters are not replaced or auto-equipped. Only an inactive character can be deleted, after typing its exact name; IDs are never reused.
5. The editing tab holds the library's save lock. Other tabs can inspect and export but cannot overwrite it. Quota failures, changed storage, and corrupt data preserve stored bytes. A blocked library offers an original-storage recovery copy for repair; this recovery file is distinct from a single-character import.

**The equipment outing remains playable:** collect Oren's kit with E beside the workshop, open **Field guide**, inspect and pin a real weapon project, then accept the riverbank materials survey. Two skitters and two samples pay 3 copper ore, 4 sunmarks, 2 fibre, and 0 XP per deliberately accepted/claimed run. The next recipe or two finite fittings connect those materials to a useful blade/bow upgrade. Returning strongest gear retains its identity/socket/Oren temper and can receive the bounded +4 fitting path. Nothing requires main-campaign advancement. [Prior results and reward matrix](development/UPGRADE_HUNT_RESULTS_2026-09-14.md).

**Both camera styles remain available:** V swaps third person and the last diorama variant; R resets the current mode. Drag orbits, wheel/pinch zooms, WASD follows the camera. Settings offers a 45–80 degree vertical FOV, default 60, and reduced motion. Each character retains its own framing. Menus and text fields consume game shortcuts.

**Compatibility:** new optional storage envelope **version 1**, key `eternities.realm10.characters.v1`, contains 1–3 validated complete worlds, monotonic character IDs, an active ID, and revision. World schema/key **9**, adventure **6**, starter **1**, pursuit **1**, optional cameraViews **1**, and levels **1–5** remain unchanged. First creation copies the active world into the envelope and leaves the old `eternities.realm10.save.v9` bytes untouched. Managed saves use only the new key. Old corrupt data is never silently replaced. Personal saves were not accessed.

Class remains unassigned. This milestone does not infer a class from equipped weapons, introduce a shared-world server, or resolve paid power, offline loss, rare-pet allocation, or construction scale. No main merge, release tag, or public deployment is authorized. Human enjoyment and camera comfort remain pending; actual footage and software checks are separate evidence.
