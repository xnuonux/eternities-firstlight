# Road After Rain — actual browser evidence

`ROAD_AFTER_RAIN.mp4` is a **92.68-second**, silent,1280×720 recording of the actual generated game. A command-earned fresh blade character reads the contract, accepts, scouts the detour, returns to dispatch, meets the delivery and claims once. Both cameras appear. The37 scripted actions use normal real-time RAF and accepted movement/UI; they do not plant objectives or grant inventory.

Chrome153.0.8010.52 reported ANGLE/NVIDIA RTX3080/D3D11. Encoded25fps is a media format, not a measured gameplay frame rate. No new GPU benchmark or human playtest is claimed. The source fixture, action list, source/build/video hashes and final story/balances are in `MEDIA_RECEIPT.json`.

The three `*_SOURCE.json` files are command-earned worlds. Their `*_JOURNEY.json` reports record production walking, commands, repeated reloads, no position edits/inventory grants/planted objectives, and accelerated test ticks. The fresh bow was crafted from gathered materials. The veteran source descends from the existing command-earned four-chapter/tempered/fitted journey. `UNPAID_EARNED.json` is a legitimately completed but unclaimed delivery; the browser suite explicitly changes its ore to9999 for a labelled synthetic capacity boundary, then checks atomic refusal and retry. These are not Dom’s saves.

`BROWSER_REPORT.json`:142 checks, zero errors, final HTML hash. PNGs are real browser screenshots: contract, delivery in both views, repaired gate and compact UI. They establish rendered behavior, not human enjoyment, visual approval or production performance.

Reproduce with `python tools/verify.py --browser`. The final review PR carries the exact pushed head, independent remote-clone result and hosted status. No public deployment was performed.
